<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 启动SpringMVC的注解功能，它会自动注册HandlerMapping、 HandlerAdapter、ExceptionResolver的相关实例 -->
    <mvc:annotation-driven/>
    
    <context:component-scan base-package="com.cxp.choosedata.controller" ></context:component-scan>

    <!-- 通用视图解析类 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
        <property name="prefix" value="/pages/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <mvc:interceptors>
        <!--下面所有的MVC映射路径都会被这个拦截器拦截-->
        <bean class="com.cxp.choosedata.controller.interceptor.AllControlerInterceptor" />
        <mvc:interceptor>
            <mvc:mapping path="/user/**" />
            <bean class="com.cxp.choosedata.controller.interceptor.MyInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>
    
</beans>